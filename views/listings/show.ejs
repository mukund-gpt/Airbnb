<% layout("layouts/boilerplate.ejs") %>
  <div class="row">
    <div class="col-8 offset-2">
      <h3 class="mt-3">
        <%= listing.title %>
      </h3>
    </div>

    <div class="card listing-card col-6 offset-2 mb-0">
      <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="Image">

      <div class="card-body">
        <p class="card-text">
          <%= listing.description %> <br><br>
            &#8377;<%= listing.price.toLocaleString("en-IN") %>/night <br>
              <%= listing.location %>,
                <%= listing.country %>
        </p>
      </div>
      <div class="btns mt-2">
        <form action="/listings/edit/<%= listing._id %>" method="get">
          <button class="btn btn-dark offset-2 add-btn">Edit</button>
        </form>
        <form action="/listings/<%= listing._id %>?_method=DELETE" method="post">
          <button class="btn btn-dark offset-4">Delete</button>
        </form>
      </div>
    </div>

    <div class="col-8 offset-2">
      <hr>
      <h3>Leave A review</h3>
      <form action="/listings/reviews/<%= listing.id%>" method="post" novalidate class="needs-validation">
        <div class="mb-3 mt-3">
          <label for="rating" class="form-label">Rating</label>
          <input type="range" name="review[rating]" id="rating" min="1" max="5" class="form-range">
        </div>
        <div>
          <label for="comment" class="form-label">Comments</label>
          <textarea name="review[comment]" id="comment" cols="20" rows="2" class="form-control" required></textarea>
          <div class="invalid-feedback">Plz, Add a Review</div>
        </div>
        <button class="btn btn-outline-dark mb-3 mt-3">Submit</button>
      </form>
      <div class="row mt-2 mb-2">
        <hr>
        <h3>All Reviews</h3>
        <div class="row">
          <% for(let review of listing.reviews){%>
            <div class="card col-md-5 m-1">
              <div class="card-body pt-1 pb-1">
                <p class="card-title"><b>Username </b>
                  <%= review.rating %>&#9734
                </p>
                <p class="card-text">
                  <%= review.comment %>
                </p>
                <form action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE" method="post">
                  <button class="btn btn-sm btn-dark">Delete</button>
                </form>
              </div>
            </div>
            <%} %>
        </div>
      </div>
    </div>
  </div>