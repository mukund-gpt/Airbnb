<% layout("layouts/boilerplate.ejs")  %>

<body>
    <h1>Listing Details</h1>
    <ul>
        <li>
            <%= listing.title %>
        </li>
        <li>
            <%= listing.description %>
        </li>
        <li>
            <img src="<%= listing.image.url %>" width="300px" alt="Image">
        </li>
        <li>
            &#8377;<%= listing.price.toLocaleString("en-IN") %>
        </li>
        <li>
            <%= listing.location %>
        </li>
        <li>
            <%= listing.country %>
        </li>

    </ul>
    <form action="/listings/edit/<%= listing._id %>" method="get"><button>Edit</button></form>
    <form action="/listings/<%= listing._id %>?_method=DELETE" method="post"><button>Delete</button></form>


</body>
